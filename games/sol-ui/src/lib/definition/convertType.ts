import { StationType } from '@tabletop/sol'

export enum ConvertType {
    SolarGate = 'SolarGate',
    EnergyNode = StationType.EnergyNode,
    SundiverFoundry = StationType.SundiverFoundry,
    TransmitTower = StationType.TransmitTower
}
