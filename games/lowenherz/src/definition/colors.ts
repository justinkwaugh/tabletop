import { Color } from '@tabletop/common'

// The real game's four colors are Rose, Gold, Purple, and Gray. The shared Color enum
// doesn't have exact Rose/Gold entries, so Pink/Yellow stand in as the closest match.
export const LowenherzColors = [Color.Pink, Color.Yellow, Color.Purple, Color.Gray]