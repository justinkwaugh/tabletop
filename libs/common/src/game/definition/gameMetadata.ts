export interface GameMetadata {
    name: string
    designer: string
    description: string
    year: string
    minPlayers: number
    maxPlayers: number
    defaultPlayerCount: number
    version: string
    beta: boolean
}
